name: ${project.name}
prefix: ${project.name}
api-version: 1.13
main: org.akadia.itemraffle.ItemRaffleMain
version: ${project.version}
git-version: ${git.commit.id.abbrev}
load: POSTWORLD
author: akadia
authors: [ lgou2w, akadia ]
website: https://akadia.org/
description: ${project.description}
depend: [ ]
softdepend: [ ]

commands:
  itemraffle:
    description: itemraffle command